apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-ocp-app.fullname" . }}
  labels:
    {{- include "node-ocp-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-ocp-app.selectorLabels" . | nindent 4 }}
